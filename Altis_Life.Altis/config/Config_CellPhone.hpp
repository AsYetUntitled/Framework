#define true 1
#define false 0

class CellPhone_Settings 
{
    sqlMessages_toggle = true; //Turns messages being saved On/Off and disables messages button when false. Default = ON.
    
    use_old_gui = false; //Uses the old gui for the cellphone when true. ON/OFF
    
    allow_msg_delete = true; //Allows players to delete messages received. ON/OFF 
};